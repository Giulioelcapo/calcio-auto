export function SiteFooter() {
  return (
    <footer className="mt-auto border-t border-[var(--line)] py-8 text-sm text-[var(--muted)]">
      <div className="fm-shell flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
        <p>PitchPulse — aggiornamento automatico da football-data.org. Zero editing manuale.</p>
        <p>Dati free delayed · Aggiornamento automatico</p>
      </div>
    </footer>
  );
}
